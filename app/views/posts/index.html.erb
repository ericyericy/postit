<%= render 'shared/title', title: "Posts List" %>

<% @posts.each do |post| %>
<div>
	<p>
		<%= post.title %>
		<br>
		<%= post.content %>
	</p>
	<span>
		<%= link_to "show post", post_path(post) %>
	</span>
</div>
<% end %>